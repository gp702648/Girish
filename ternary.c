#include<stdio.h>
void main()
{
    int a;
    float b;
    char c;
    double d;
    printf("%d is =a\n",sizeof(a));
    printf("%d is =a\n",sizeof(b));
    printf("%d is =a\n",sizeof(c));
    printf("%d is =a",sizeof(d));
    printf("%d is =a",sizeof(main));
}
